for (let i = 1;  i < 51; i++ ) {
    // De uno al 50
    console.log(i);
    }

const array = ['En', 'un', 'lugar', 'de', 'la', 'mancha'];
const len = array.length; 
// recorre un array mediante un for clásico
for (let i = 0; i < len; i++) {
// En, un, lugar, de, la, mancha (en distintas líneas)
    console.log(array[i]);
    document.write(array[i] + "")
}

// ejercicio 9 tablas de multiplicar
for (let i = 0; i < 11; i++) {
    //Tabla del 5X1 a 5x10)
    document.write
        console.log(array[i]);
        document.write("5x"+ i + "=" + 5*1 +  "<br>")
    }

    // Ejercicio 10
var palabra = prompt("Escribe cualquier palabra")